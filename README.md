# Iskele
Iskele is my personal infrastructure and cybersecurity playground. It hosts my website, tools, automations, remote-access systems, and various experiments running across Docker, Kubernetes, Cloudflare, and security-focused services.


ISKELE PRIIMARY LOG
update - sudo apt update && sudo apt upgrade -y && sudo apt dist-upgrade -y && sudo apt autoremove -y && sudo apt autoclean -y
ssh installed and enabled - sudo apt install -y ssh && sudo systemctl enable ssh --now
rdp 
nomachine
cloudflare tunnel 
docker 
vmware 

CONTAINERS 
	beszel uptime-kuma guacamole firefox remmina
